toolName: view_folder
            
status: success
          
/Users/ruimve/Documents/virs/virs
          

        
            
toolName: view_files
            
status: success
          
            
filePath: /Users/ruimve/Documents/virs/virs/Cargo.toml
          
## 新架构目标回顾

### 目录结构（Monorepo）

```
virs/
├── crates/
│   ├── libs/                    # 工具包层（无业务逻辑，纯基础设施）
│   │   ├── virs-types           # 核心类型 + trait 定义（0 内部依赖）
│   │   ├── virs-config          # 配置加载（→ types）
│   │   ├── virs-utils           # 通用工具：加密/JWT/认证（独立）
│   │   ├── virs-models          # 数据库模型 + FromRow（→ types）
│   │   ├── virs-ccxt            # 交易所底层适配（→ types）
│   │   └── virs-exchange        # 交易所上层封装 + Paper 模式（→ types, ccxt, models）
│   └── services/                # 服务层（业务逻辑，依赖 libs）
│       ├── virs-market          # K线引擎 + 聚合（→ types, ccxt, exchange）
│       ├── virs-position        # 持仓引擎 + 风控（→ types, exchange）
│       ├── virs-bot             # 交易策略（→ types, models）
│       │   ├── grid/            #   网格交易
│       │   ├── auto/            #   自动交易（LLM 决策）
│       │   └── common/          #   共享：AI 客户端 + 指标计算
│       └── virs-api             # HTTP/WebSocket API（→ 全部 libs + bot/market）
├── app/
│   └── virs-app                 # 应用入口：组装 + 启动（→ 全部）
├── frontend/                    # React 前端
├── migrations/                  # SQL 迁移
├── Dockerfile                   # 多阶段构建
└── docker-compose.yml           # 一键启动
```

### 依赖 DAG（无环，严格分层）

```
Layer 0: virs-types
Layer 1: virs-config, virs-models, virs-utils
Layer 2: virs-ccxt
Layer 3: virs-exchange
Layer 4: virs-market, virs-position, virs-bot
Layer 5: virs-api
Layer 6: virs-app
```

### 核心设计原则

| 原则 | 实现方式 |
|---|---|
| **依赖注入** | trait 定义在 libs 层（`ExchangePe`/`OrderExecutor`/`CredentialStore`/`LlmProviderResolver`/`GridStore`/`AutoStore`），实现在 app/adapters |
| **接口隔离** | 每个 trait 只暴露必要方法，实现者按需组合 |
| **无环依赖** | 严格分层，低层不依赖高层，services 之间不互相依赖 |
| **配置驱动** | 环境变量控制运行模式（Paper/Real），`virs-config` 统一加载 |
| **Paper 模式** | `PaperExchangeAdapter` 实现完整 `ExchangePe`，懒加载真实余额，内存撮合 |
| **错误类型化** | 底层用 `thiserror`（`PositionEngineError`/`BotError`），上层用 `anyhow` |
| **Stream 抽象** | `ExchangePe::subscribe_order_updates` 返回 `OrderUpdateStream`（`Pin<Box<dyn Stream>>`），virs-types 不依赖 tokio |

### 各 crate 职责

| Crate | 职责 | 关键 trait/类型 |
|---|---|---|
| **virs-types** | 核心类型 + 接口定义 | `ExchangePe`, `OrderExecutor`, `CredentialStore`, `LlmProviderResolver`, `BotError`, `OrderUpdateStream` |
| **virs-config** | 环境变量 → 结构化配置 | `AppConfig`, `AiConfig` |
| **virs-utils** | 加密/JWT/密码哈希 | `crypto::encrypt/decrypt`, `auth::verify_token` |
| **virs-models** | DB 模型 + SQL 映射 | `GridBot`, `AutoBot`, `Kline`, `User` |
| **virs-ccxt** | 交易所 REST/WS 底层 | `BinanceExchange`, `BybitExchange`, `OkxExchange` |
| **virs-exchange** | 交易所统一封装 + Paper | `ExchangeRegistry`, `CcxtExchangeAdapter`, `PaperExchangeAdapter` |
| **virs-market** | K线聚合 + 缓存 + 补缺 | `KlineEngine`, `ExchangeKlineSource` |
| **virs-position** | 持仓/订单/风控/PnL | `PositionEngine`, 4 并行循环 |
| **virs-bot** | 网格 + 自动交易策略 | `GridEngine`, `AutoEngine`, `GridWorker`, `AutoWorker` |
| **virs-api** | HTTP API + WebSocket | Axum handlers, JWT 中间件, WS 广播 |
| **virs-app** | 组装 + 启动 | adapters 目录下所有 trait 实现 |

### 当前完成状态

- Monorepo 架构：**已完成**
- DAG 无环：**已验证**
- 依赖清理（tokio/anyhow 从 virs-types 移除）：**已完成**
- Docker 一键启动：**已完成**
- 前端：**已有**（React + Vite）
- 所有 TODO 接口实现：**已完成**

### 注释掉的 `virs-ai`

workspace 中有 `# "crates/libs/virs-ai"` 被注释，标注 "Phase 5+"。当前 AI 能力（LLM 调用、提示工程）内嵌在 `virs-bot/common/ai_client.rs` 中。如果未来需要多策略共享 AI 基础设施（如 RAG、多模型路由），可提取为独立 crate。