## 目录结构（Monorepo）

```
virs/
├── crates/
│   ├── libs/                    # 工具包层（无业务逻辑，纯基础设施）
│   │   ├── virs-error           # 统一错误类型 + 分类 trait（0 内部依赖）
│   │   ├── virs-types           # 核心类型 + 端口 trait 定义（→ error）
│   │   ├── virs-config          # 配置加载（→ error）
│   │   ├── virs-utils           # 通用工具：加密/JWT/认证（→ error）
│   │   ├── virs-models          # 数据库模型 + FromRow（→ types）
│   │   ├── virs-ccxt            # 交易所底层适配（→ types, error）
│   │   └── virs-exchange        # 交易所上层封装 + Paper 模式（→ types, ccxt, models, error）
│   └── services/                # 服务层（业务逻辑，依赖 libs）
│       ├── virs-market          # K线引擎 + 订单簿引擎 + 聚合（→ types, ccxt, exchange, error）
│       ├── virs-position        # 持仓引擎 + 风控 + PnL（→ types, exchange, error）
│       ├── virs-bot             # 交易策略（→ types, models, error）
│       │   ├── grid/            #   网格交易（engine, worker, ai, ports, types, utils）
│       │   ├── auto/            #   自动交易（engine, worker, ai, strategy, ports, types）
│       │   └── common/          #   共享：AI 客户端 + 指标计算 + 端口
│       └── virs-api             # HTTP/WebSocket API（→ 全部 libs + bot/market）
├── app/
│   └── virs-app                 # 应用入口：组装 + 启动 + adapters（→ 全部）
├── frontend/                    # React + Vite 前端
├── migrations/                  # SQL 迁移
├── Dockerfile                   # 多阶段构建
└── docker-compose.yml           # 一键启动
```

## 依赖 DAG（无环，严格分层）

每个 crate 的层级 = max(所有 path 依赖的层级) + 1，virs-error 为 Layer 0。

```
Layer 0  virs-error                              (基础错误层，0 内部依赖)
           │
   ┌───────┼───────────┐
   ▼       ▼           ▼
Layer 1  virs-types  virs-config  virs-utils     (→ error)
           │
   ┌───────┴───────────┐
   ▼                   ▼
Layer 2  virs-models   virs-ccxt                  (→ types [+ error])
           │               │
   ┌───────┘               │
   ▼                       ▼
Layer 3  virs-bot          virs-exchange           (→ types, models, error / → types, ccxt, models, error)
   │                       │
   │           ┌───────────┤
   │           ▼           ▼
   │     Layer 4  virs-market  virs-position       (→ types, ccxt, exchange, error / → types, exchange, error)
   │                   │
   └───────────────────┤
                       ▼
Layer 5          virs-api                          (→ types, error, models, utils, bot, exchange, market, ccxt)
                       │
                       ▼
Layer 6          virs-app                          (→ 全部，唯一二进制入口 bin=virs)
```

依赖方向严格单向：libs（L0–L2）→ services（L3–L5）→ app（L6）。没有任何 lib 依赖 service，没有任何 service 依赖 app。`virs-error` 是整个 DAG 的唯一根，`virs-app` 是唯一入度为 0 的顶层节点（组合根）。

## 核心设计原则

| 原则 | 实现方式 |
|---|---|
| **依赖注入** | 端口 trait 定义在 virs-types（`ExchangePe`/`OrderExecutor`/`CredentialStore`/`LlmProviderResolver`/`GridStore`/`AutoStore`/`PriceProvider`/`MarketDataProvider`），业务端口实现在 `virs-app/adapters/`，交易所适配器（`PaperExchangeAdapter`/`CcxtExchangeAdapter`）实现在 virs-exchange |
| **接口隔离** | 每个 trait 只暴露必要方法，实现者按需组合；GridWorker 和 AutoWorker 各持所需端口的 `Arc<dyn Trait>` |
| **无环依赖** | 严格分层，低层不依赖高层，services 之间不互相依赖（virs-bot 不依赖 virs-exchange，virs-market 不依赖 virs-position） |
| **配置驱动** | 环境变量控制运行模式（Paper/Real），`virs-config` 统一加载为 `AppConfig`，引擎层通过 `AppConfig.paper` 消费 |
| **Paper 模式** | `PaperExchangeAdapter` 实现完整 `ExchangePe`，懒加载真实余额作为初始资金，内存撮合，通过 `on_price_tick` 驱动限价单成交 |
| **错误类型化** | 所有错误类型用 `thiserror` 定义在 virs-error（Layer 0），顶层 `VirsError` 通过 `#[from]` 统一收敛；全仓零自定义错误类型、零 `Result<_, String>`、零直接 anyhow 调用 |
| **Stream 抽象** | `ExchangePe::subscribe_order_updates` 返回 `OrderUpdateStream`（`Pin<Box<dyn Stream>>`），virs-types 不直接依赖 tokio（使用 `futures-core`） |

## 各 crate 职责

| Crate | 层级 | 职责 | 关键类型/trait |
|---|---|---|---|
| **virs-error** | L0 | 统一错误类型 + 分类 + HTTP 集成 | `VirsError`, `BotError`, `PositionEngineError`, `ExchangeError`, `ApiError`, `Retryable`/`Categorized`/`HttpStatus`/`ErrorCode`, `Context`, `VirsResult`/`BotResult` |
| **virs-types** | L1 | 核心类型 + 端口 trait 定义 | `ExchangePe`, `OrderExecutor`, `CredentialStore`, `LlmProviderResolver`, `GridStore`, `AutoStore`, `PriceProvider`, `MarketDataProvider`, `OrderUpdateStream`, `EngineCommand`, `EngineEvent`, `Position`, `MarketSnapshot` |
| **virs-config** | L1 | 环境变量 → 结构化配置 | `AppConfig`, `AiConfig`（唯一定义位置）, `load_config_from_env → VirsResult` |
| **virs-utils** | L1 | 加密/JWT/密码哈希 | `crypto::encrypt/decrypt/derive_key/hash_password → VirsResult`, `auth::encode_jwt/decode_jwt → VirsResult` |
| **virs-models** | L2 | DB 模型 + SQL 映射 | `GridBot`, `AutoBot`, `Order` |
| **virs-ccxt** | L2 | 交易所 REST/WS 底层 | `Exchange` trait, `BinanceExchange`（api/fapi/sapi/ws）, `create_exchange()` 工厂 |
| **virs-exchange** | L3 | 交易所统一封装 + Paper | `Exchanges`（注册表）, `CcxtExchangeAdapter`, `PaperExchangeAdapter`, `Exchange`（应用层 trait） |
| **virs-bot** | L3 | 网格 + 自动交易策略 | `GridEngine`/`AutoEngine`, `GridWorker`/`AutoWorker`, `GridAiService`/`AutoAiService`, `GridCommand`/`AutoCommand` |
| **virs-market** | L4 | K线采集 + 聚合 + 缓存 + 补缺 | `KlineEngine`, `OrderBookEngine`, `Aggregator`, `GapDetector`, `SymbolCache` |
| **virs-position** | L4 | 持仓/订单/风控/PnL | `PositionEngine`, `RiskChecker`, `PnlTracker`, `PositionPersistence`, `persist!` 宏 |
| **virs-api** | L5 | HTTP API + WebSocket | Axum router, JWT 中间件, 3 个 WS handler (`/ws/kline`、`/ws/orderbook`、`/ws/position`), `EngineManager` trait |
| **virs-app** | L6 | 组装 + 启动 | `AppEngineManager`, adapters（`PgGridStore`/`PgAutoStore`/`PeOrderExecutor`/`PgCredentialStore`/`DefaultLlmResolver`/`ExchangePriceProvider`/`ExchangeMarketDataProvider`） |

## 统一错误处理架构

virs-error 是全仓唯一的错误定义 crate，workspace 其他 10 个 crate 零 thiserror 依赖、零自定义错误类型。

### 错误类型层级

```
ExchangeError ──#[from]──→ PositionEngineError ──#[from]──→ VirsError
       │                          │
       └──────────#[from]─────────┤
                                  │
BotError ──────────#[from]───────→│
                                  │
sqlx::Error ───────#[from]───────→│  (feature = "sqlx")
anyhow::Error ─────#[from]───────→│  (Other 变体，兜底)
                                  │
Http{status, message} ───────────→│
Config(String) ──────────────────→│
Auth(String) ────────────────────→│
Crypto(String) ──────────────────→│
                                  ▼
                              VirsError
```

### VirsError 变体

| 变体 | 字段 | `#[from]` | feature 门控 |
|---|---|---|---|
| `Bot` | `BotError` | 是 | — |
| `Position` | `PositionEngineError` | 是 | — |
| `Exchange` | `ExchangeError` | 是 | — |
| `Database` | `sqlx::Error` | 是 | `sqlx` |
| `Http` | `{ status: u16, message: String }` | 否 | — |
| `Config` | `String` | 否 | — |
| `Auth` | `String` | 否 | — |
| `Crypto` | `String` | 否 | — |
| `Other` | `anyhow::Error` | 是 | — |

### 分类 trait

四个 trait 在每个错误类型上独立实现，`VirsError` 通过 match 委托给内部领域错误：

| Trait | 方法 | 用途 |
|---|---|---|
| `Retryable` | `is_retryable() -> bool` | 重试循环决策（Database/Network/RateLimited → true） |
| `Categorized` | `category() -> ErrorCategory` | 错误分类（Network/RateLimited/Authentication/Validation/NotFound/Conflict/Database/Config/Internal） |
| `HttpStatus` | `http_status() -> u16` | HTTP 状态码映射（401/400/404/409/429/500/502/503） |
| `ErrorCode` | `error_code() -> &'static str` | 机器可读错误码（如 `DATABASE_ERROR`/`EXCHANGE_RATE_LIMITED`/`PE_RISK_CHECK_FAILED`） |

### Context re-export

```rust
// virs-error/src/context.rs
pub use anyhow::Context;
```

调用方通过 `use virs_error::Context` 获得 `.context("...")` 能力，无需在自己的 Cargo.toml 中引入 anyhow。配合 `VirsError::Other(#[from] anyhow::Error)`，`?` + `.context()` 可将任意错误收敛进 VirsError。全 workspace 仅 virs-error 的 Cargo.toml 声明 anyhow 依赖。

### HTTP 边界脱敏

`IntoResponse`（`#[cfg(feature = "axum")]`）仅实现于 `VirsError`：用 `http_status()` 取状态码，body 为 `ApiError` JSON。`ApiError::from(VirsError)` 对 5xx 强制替换 message 为 `"Internal server error"`，4xx 透传 `to_string()`。domain error 必须先通过 `#[from]` 提升为 `VirsError` 才能在 axum handler 边界返回。

### Feature 按需启用

| Feature | 启用内容 | 哪些 crate 开启 |
|---|---|---|
| `axum` | `IntoResponse` 实现 | virs-api |
| `sqlx` | `Database(#[from] sqlx::Error)` 变体 | virs-types, virs-position, virs-api, virs-app |
| `reqwest` | `BotError::Reqwest`/`ExchangeError::From<reqwest::Error>` | virs-ccxt, virs-bot |

default 为空，遵循"谁用谁开"原则，避免库层引入不必要的重依赖。

## 端口与适配器

### 端口 trait（定义在 virs-types）

| Trait | 方法 | 返回类型 |
|---|---|---|
| `ExchangePe` | `get_ticker/get_balance/get_positions/place_order/cancel_order/cancel_all_orders/set_leverage/get_position_mode/get_funding_rate/subscribe_order_updates/on_price_tick/restore_positions` | `VirsResult<T>` |
| `OrderExecutor` | `send_command(OrderCommand)` / `query_open_position(symbol)` | `BotResult<T>` |
| `CredentialStore` | `load_credentials(user_id)` | `BotResult<Vec<(provider, api_key, model)>>` |
| `LlmProviderResolver` | `is_available()` / `resolve(user_credentials)` | `BotResult<(api_key, base_url, model, provider)>` |
| `PriceProvider` | `get_price(exchange, symbol)` | `Option<f64>` |
| `MarketDataProvider` | `get_market_snapshot(exchange, symbol)` / `get_account_balance(exchange)` | `MarketSnapshot` / `AccountBalance` |
| `GridStore` | `load_running_bots/load_bot/load_trades/record_open_trade/close_trade/find_open_trade/save_stats/update_bot_status/update_grid_params/save_analysis_log/delete_bot` | `VirsResult<T>` |
| `AutoStore` | `load_running_bots/load_bot/record_open_trade/close_trade/update_trade_stop_loss/find_open_trade/mark_trade_orphaned/find_last_closed_trade/load_consecutive_losses/save_analysis_log/delete_bot` | `VirsResult<T>` |

### 适配器实现（定义在 virs-app/adapters）

| 适配器 | 实现端口 | 数据源 |
|---|---|---|
| `PgGridStore` | `GridStore` | `qd_grid_bots`/`pe_grid_order_context`+`pe_orders`/`qd_grid_analysis_logs` |
| `PgAutoStore` | `AutoStore` | `qd_auto_bots`/`pe_auto_order_context`+`pe_orders`/`qd_auto_analysis_logs` |
| `PeOrderExecutor` | `OrderExecutor` | 转发 `EngineCommand` 给 PositionEngine；spawn 桥接任务把 `EngineEvent` 转 `OrderEvent` |
| `PgCredentialStore` | `CredentialStore` | `qd_ai_credentials`（AES-GCM 加密） |
| `DefaultLlmResolver` | `LlmProviderResolver` | 优先级 deepseek > openai > openrouter；用户凭证优先于系统配置 |
| `ExchangePriceProvider` | `PriceProvider` | KlineEngine 1m close → exchange ticker 回退 |
| `ExchangeMarketDataProvider` | `MarketDataProvider` | KlineEngine 多周期缓存 → REST 回退；余额用 PE exchange（Paper）或 Exchanges |

交易所适配器（定义在 virs-exchange）：`PaperExchangeAdapter` 和 `CcxtExchangeAdapter` 均实现 `ExchangePe`，在 `AppEngineManager.ensure_started()` 中根据 paper_mode 选择注入。

## 运行时架构

### 惰性初始化

交易引擎（Position/Grid/Auto）在 boot 时不启动。`main.rs` 完成配置加载、数据库迁移、管理员创建、行情引擎构造后，调用 `engine_manager.restore_if_needed()`——仅当 DB 中存在 running bot 时才触发 `ensure_started()`，惰性启动三大引擎。`AppEngineManager` 用 `AtomicBool` + `Mutex<()>` + `OnceLock` 双重检查锁定确保只初始化一次。

### PositionEngine（virs-position/src/engine.rs）

核心数据结构：`DashMap<(exchange, symbol, side), Position>` 内存仓位、`DashMap<Uuid, PositionOrder>` 订单索引、`broadcast::Sender<EngineEvent>` 事件广播。

`run()` 启动 2 个并行 tokio 任务：

| 任务 | 职责 |
|---|---|
| `command_loop` | 消费 `EngineCommand` mpsc，分发到 `handle_open_position`/`handle_close_position`/`handle_place_order`/`handle_cancel_all_orders`/`handle_price_tick` 等 handler |
| `ws_feed_loop` | 消费 `WsFeedEvent::OrderUpdate`（真实交易所 WS 推送）/`ConnectionChanged`，调 `handle_ws_order_update` |

`persist!` 宏：持久化操作最多 3 次重试，每次失败 `warn`，3 次后 `error` 并放弃（不 panic），重试间隔 `100ms * attempts`。

`recover_lock` 模式：锁中毒时 `error!` 后 **panic**（拒绝返回可能不一致的脏数据，"宁崩溃也不用陈旧状态"，避免交易系统数据损坏）。当前所有持锁路径均使用 `recover_lock`（panic 式），`PositionEngineError` 中不再有 `LockPoisoned` 变体——锁中毒被视为不可恢复的内部错误，直接 panic 让进程重启，而非向上传播错误。

### GridEngine + GridWorker（virs-bot/src/grid/）

`GridEngine` 是 bot 生命周期管理器，自身不交易：持有 workers `HashMap<Uuid, JoinHandle>`，`run()` 先 `restore_running_bots()` 再 `while let Some(cmd) = cmd_rx.recv()` 分发 `GridCommand`（StartBot/StopBot/DeleteBot）。`start_bot` 创建 shutdown/adjust channel 后 `tokio::spawn(GridWorker::run())`。

`GridWorker` 单 bot 执行循环：
- 重试 10 次拉初始价 → `load_existing_trades()` 恢复层级持仓 → 若网格参数空触发初始 LLM 分析 → `place_initial_orders()`
- 主循环 `tokio::select!`：shutdown / adjust_signal / 5s price_tick / LLM 周期信号 / OrderEvent
- `on_price_tick`：按 4 种条件（buy 开仓/sell 开仓/buy 平仓/sell 平仓）遍历 GridLevel 挂单
- `on_llm_decision`：拉 `MarketSnapshot` → `GridAiService` 调 LLM → 解析高斯分布网格参数 → `update_grid_params` + `calculate_levels`

### AutoEngine + AutoWorker（virs-bot/src/auto/）

与 GridEngine 对称，差异：额外持有 `pe_event_tx`，`start_bot` 时 `subscribe()` 传给 AutoWorker（直接订阅 PE 事件，不通过 OrderEvent 桥接）。

`AutoWorker` 单 bot 执行循环比 GridWorker 更复杂：
- 重启恢复：`load_consecutive_losses` → `find_last_closed_trade`（冷却期防重入）→ 孤儿 trade 检测 → 若有 position_id 则从 DB 恢复 + `refresh_position_from_pe` + 15s 超时等待 PE 事件
- 主循环 `tokio::select!`：shutdown / 5s price_tick（trailing stop / 持仓超时 / 止损止盈检查）/ LLM 周期信号 / OrderEvent / `EngineEvent`
- `on_llm_decision`：`AutoDecision{action, reason, confidence, market_regime}` → `OpenLong/OpenShort/ClosePosition/Hold`

### Paper 模式

`PaperExchangeAdapter` 纯内存模拟交易：
- `place_order`：市价单立即用 `last_prices` 撮合，限价单存入 `pending` DashMap
- `on_price_tick(symbol, price)`：更新 `last_prices`，遍历 `pending` 限价单，买单价≥price 或卖单价≤price 则撮合，发 `WsFeedEvent::OrderUpdate`
- `update_position_on_fill`：处理开仓/平仓/部分平仓/反转四种情况，计算已实现盈亏、加权平均入场价、保证金、`compute_paper_liquidation_price`
- `restore_positions`：进程重启后从 DB 恢复内存仓位

`AppEngineManager` 在 Paper 模式下 spawn 3 秒 ticker，从 KlineEngine 取价格后 `pe_cmd_tx.send(EngineCommand::PriceTick{symbol, price})`，驱动 Paper 限价单撮合。

### KlineEngine + OrderBookEngine（virs-market）

`KlineEngine`：WS 实时 K 线采集 + 多周期聚合 + 缺口检测回补。`subscribe(exchange, symbol, market_type)` 惰性自动 `start()`，启动两个任务：WS update processor（1m 更新 + `Aggregator` 聚合到 5m/15m/1h/4h/1d + 持久化 + `KlineEvent` 广播）和 Gap checker（60s 间隔回补）。`SymbolCache` 按时间框架存储 candle 数组。

`OrderBookEngine`：镜像架构但更简单（无聚合/无缺口检测/无持久化），每条 WS 消息是完整 top-N 快照，直接广播 `OrderBookEvent`。

## 关键数据流

### 行情数据流

```
Binance WS (perpetual)
  → BinanceKlineWs / BinanceOrderBookWs (virs-ccxt)
  → WsEvent broadcast → KlineEngine: 更新 SymbolCache(M1) + Aggregator 聚合高周期
  → broadcast KlineEvent / OrderBookEvent
  → 三类消费者:
     1. /ws/kline、/ws/orderbook → 前端
     2. PriceProvider.get_price() → GridWorker/AutoWorker price_tick
     3. MarketDataProvider.get_market_snapshot() → LLM 决策
```

### 交易决策与下单流（以 Auto 为例）

```
AutoWorker price_tick / LLM 周期信号
  → on_llm_decision():
     MarketDataProvider.get_market_snapshot() → indicators_json
     OrderExecutor.query_open_position(symbol) → 当前仓位
     AutoAiService → LlmProviderResolver.resolve() → call_llm_api
     → AutoDecision{action, reason, confidence, market_regime}
  → execute_decision():
     OpenLong/OpenShort → OrderExecutor.send_command(OpenPosition)
       → PeOrderExecutor 转 EngineCommand::OpenPosition → pe_cmd_tx
       → PositionEngine command_loop → handle_open_position
         → exchange.set_leverage + exchange.place_order
         → (Paper) update_position_on_fill / (Real) WS 推送
         → emit EngineEvent::PositionOpened/OrderFilled
     → AutoWorker 通过 pe_event_rx 收到 → 更新 current_position + store.record_open_trade
```

### PE 事件广播多路复用

`PositionEngine.event_sender()` 是单一 `broadcast::Sender<EngineEvent>`，被多个消费者 `subscribe()`：
- `PeOrderExecutor`（Grid/Auto 各一个）桥接成 `OrderEvent` → Worker `event_rx`
- AutoWorker 额外直接订阅（感知 `PositionUpdated`/`PositionClosed` 等非订单事件）
- `AppEngineManager.pe_event_subscribe()` → `/ws/position` handler
- `broadcast` 通道特性：lagged 消费者丢消息但继续运行（各 Worker 有 Lagged 处理）

### 重启恢复流

```
main.rs → engine_manager.restore_if_needed()
  → 解密 qd_exchange_credentials → 注册 CcxtAdapter 到 Exchanges
  → 查 status='running' bot 的 symbols → kline_engine.subscribe + orderbook_engine.subscribe
  → ensure_started(paper_mode)
     → PositionEngine.run(): recover_state (pe_orders 聚合 → 内存) + restore_positions (Paper)
     → GridEngine/AutoEngine.run(): restore_running_bots → start_bot
        → Worker.run(): 恢复 levels/trades/consecutive_losses/last_close_event/current_position
```

## HTTP API 与 WebSocket

### HTTP 路由（`/api/*` 前缀，CorsLayer + SPA fallback）

| 域 | 路由 |
|---|---|
| Health | `GET /api/health` |
| Auth | `POST /api/user/login\|logout`、`GET /api/user/info` |
| Users | `GET /api/users/list`、`POST /api/users/create`、`PUT /api/users/update`、`DELETE /api/users/delete` |
| Market | `GET /api/market/ticker\|klines\|orderbook\|balances\|symbols` |
| Kline Engine | `POST /api/kline/subscribe`、`GET /api/kline/data`（1m/5m/15m/1h/4h/1d） |
| OrderBook Engine | `POST /api/orderbook/subscribe` |
| Credentials | `GET /api/credentials/list\|test\|status`、`POST /api/credentials/save\|verify`、`DELETE /api/credentials/delete/{id}` |
| AI | `GET /api/ai/status`、`POST /api/ai/optimize\|explain\|recommend-strategy` |
| AI Credentials | `GET /api/ai-credentials/list\|test\|models\|balance`、`POST /api/ai-credentials/save`、`DELETE /api/ai-credentials/delete/{id}` |
| Grid Bot | `POST /api/grid/create`、`GET /api/grid/list\|{id}\|{id}/analysis-logs\|{id}/trades\|{id}/stats`、`POST /api/grid/{id}/start\|stop`、`DELETE /api/grid/{id}/delete` |
| Auto Bot | 对称的 `/api/auto/*` |
| System | `GET /api/system/paper/status\|info` |

### WebSocket 端点

| 端点 | 数据源 | 客户端可过滤 |
|---|---|---|
| `/ws/kline` | `KlineEngine::subscribe_events()` | 按 timeframe 过滤 |
| `/ws/orderbook` | `OrderBookEngine::subscribe_events()` | 按 symbol 过滤 |
| `/ws/position` | `EngineManager::pe_event_subscribe()`（`EngineEvent`） | 按 symbol 过滤；订阅时推送当前快照 |

## 交易所扩展性设计

virs-ccxt 采用适配器模式，每个交易所实现独立隔离。当前仅 Binance 已实现（api/fapi/sapi + kline_ws/order_ws/orderbook_ws），Bybit/OKX 为计划中。添加新交易所的步骤：

1. 在 `adapter/` 下创建新模块（如 `adapter/bybit/mod.rs`）
2. 实现 `Exchange` trait
3. 在 `adapter/mod.rs` 中注册模块
4. 在 `create_exchange()` 工厂函数中添加 match 分支

所有交易所特定逻辑（REST 签名、WS 解析、错误映射）完全隔离在各自模块中，共享统一的 `Exchange` trait 接口。上层通过 `Exchanges` 注册表按 `"{name}:{market_type}"` 查找，`CcxtExchangeAdapter` 将应用层 `Exchange` 适配为 PE 层 `ExchangePe`。

## 注释掉的 `virs-ai`

workspace 根 Cargo.toml 中 `# "crates/libs/virs-ai"` 被注释，标注 "Phase 5+"，且该目录在文件系统中不存在。当前 AI 能力（LLM 调用、提示工程）内嵌在 `virs-bot/common/ai_client.rs` 中，各策略有独立的 AI service（`grid/ai.rs`/`auto/ai.rs`）和 Prompt 模板（`grid/utils/prompt.rs`/`auto/types.rs`）。如果未来需要多策略共享 AI 基础设施（如 RAG、多模型路由），可提取为独立 crate。
