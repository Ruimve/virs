## 新架构目标回顾

### 目录结构（Monorepo）

```
virs/
├── crates/
│   ├── libs/                    # 工具包层（无业务逻辑，纯基础设施）
│   │   ├── virs-error           # 统一错误类型 + 分类 trait（0 内部依赖）
│   │   ├── virs-types           # 核心类型 + trait 定义（→ error）
│   │   ├── virs-config          # 配置加载（→ error, types）
│   │   ├── virs-utils           # 通用工具：加密/JWT/认证（→ error）
│   │   ├── virs-models          # 数据库模型 + FromRow（→ types）
│   │   ├── virs-ccxt            # 交易所底层适配（→ types, error）
│   │   └── virs-exchange        # 交易所上层封装 + Paper 模式（→ types, ccxt, models, error）
│   └── services/                # 服务层（业务逻辑，依赖 libs）
│       ├── virs-market          # K线引擎 + 聚合（→ types, ccxt, exchange, error）
│       ├── virs-position        # 持仓引擎 + 风控（→ types, exchange, error）
│       ├── virs-bot             # 交易策略（→ types, models, error）
│       │   ├── grid/            #   网格交易
│       │   ├── auto/            #   自动交易（LLM 决策）
│       │   └── common/          #   共享：AI 客户端 + 指标计算
│       └── virs-api             # HTTP/WebSocket API（→ 全部 libs + bot/market）
├── app/
│   └── virs-app                 # 应用入口：组装 + 启动（→ 全部）
├── frontend/                    # React 前端
├── migrations/                  # SQL 迁移
├── Dockerfile                   # 多阶段构建
└── docker-compose.yml           # 一键启动
```

### 依赖 DAG（无环，严格分层）

```
Layer 0: virs-error (统一错误类型：BotError/PositionEngineError/ExchangeError/VirsError，分类 trait，HTTP 集成，Database 变体)
         virs-types (核心类型 + trait 定义；依赖 virs-error，所有 trait 返回 VirsResult/BotResult/PositionResult)
Layer 1: virs-config (→ error, types), virs-models (→ types), virs-utils (→ error)
Layer 2: virs-ccxt (→ types, error)
Layer 3: virs-exchange (→ types, ccxt, models, error)
Layer 4: virs-market (→ types, ccxt, exchange, error), virs-position (→ types, exchange, error), virs-bot (→ types, models, error)
Layer 5: virs-api (→ 全部 libs + bot/market, error[axum+sqlx])
Layer 6: virs-app (→ 全部)
```

### 核心设计原则

| 原则 | 实现方式 |
|---|---|
| **依赖注入** | trait 定义在 libs 层（`ExchangePe`/`OrderExecutor`/`CredentialStore`/`LlmProviderResolver`/`GridStore`/`AutoStore`），业务端口 trait 实现在 app/adapters，交易所适配器（`PaperExchangeAdapter`/`CcxtExchangeAdapter`）实现在 virs-exchange |
| **接口隔离** | 每个 trait 只暴露必要方法，实现者按需组合 |
| **无环依赖** | 严格分层，低层不依赖高层，services 之间不互相依赖 |
| **配置驱动** | 环境变量控制运行模式（Paper/Real），`virs-config` 统一加载为 `AppConfig`，引擎层通过 `AppConfig.paper` 消费（不再直接读取环境变量） |
| **Paper 模式** | `PaperExchangeAdapter` 实现完整 `ExchangePe`，懒加载真实余额，内存撮合 |
| **错误类型化** | 所有错误类型（`BotError`/`PositionEngineError`/`ExchangeError`）用 `thiserror` 定义在 virs-error（Layer 0），顶层 `VirsError` 通过 `#[from]` 统一收敛；各 crate 直接 `use virs_error::{...}` 导入，不做 re-export；上层用 `anyhow` + `virs_error::Context` 补充上下文；virs-error 提供分类 trait（`Retryable`/`Categorized`/`HttpStatus`/`ErrorCode`）与 axum `IntoResponse` 实现 |
| **Stream 抽象** | `ExchangePe::subscribe_order_updates` 返回 `OrderUpdateStream`（`Pin<Box<dyn Stream>>`），virs-types 不直接依赖 tokio（使用 `futures-core`）；注意：可选的 sqlx feature（默认启用）会通过 `runtime-tokio-rustls` 间接引入 tokio |

### 各 crate 职责

| Crate | 职责 | 关键 trait/类型 |
|---|---|---|
| **virs-error** | 统一错误类型 + 分类 + HTTP 集成 | `BotError`, `PositionEngineError`, `ExchangeError`, `VirsError`, `Retryable`/`Categorized`/`HttpStatus`/`ErrorCode`, `ApiError`, `Context` |
| **virs-types** | 核心类型 + 接口定义 | `ExchangePe`, `OrderExecutor`, `CredentialStore`, `LlmProviderResolver`, `GridStore`, `AutoStore`, `OrderUpdateStream` |
| **virs-config** | 环境变量 → 结构化配置 | `AppConfig`, `AiConfig`（唯一定义位置），`load_config_from_env → VirsResult` |
| **virs-utils** | 加密/JWT/密码哈希 | `crypto::encrypt/decrypt → VirsResult`, `auth::encode_jwt/decode_jwt → VirsResult` |
| **virs-models** | DB 模型 + SQL 映射 | `GridBot`, `AutoBot`, `Kline`, `User` |
| **virs-ccxt** | 交易所 REST/WS 底层 | `BinanceExchange`（已实现），`BybitExchange`/`OkxExchange`（计划中，架构支持快速扩展） |
| **virs-exchange** | 交易所统一封装 + Paper | `Exchanges`, `CcxtExchangeAdapter`, `PaperExchangeAdapter` |
| **virs-market** | K线聚合 + 缓存 + 补缺 | `KlineEngine`, `ExchangeKlineSource` |
| **virs-position** | 持仓/订单/风控/PnL | `PositionEngine`, 4 并行循环（由 virs-app 直接组装，不经 virs-api 暴露） |
| **virs-bot** | 网格 + 自动交易策略 | `GridEngine`, `AutoEngine`, `GridWorker`, `AutoWorker` |
| **virs-api** | HTTP API + WebSocket | Axum handlers, JWT 中间件, WS 广播 |
| **virs-app** | 组装 + 启动 | adapters 目录下业务端口 trait 实现（`PgGridStore`/`PgAutoStore`/`PeOrderExecutor`/`PgCredentialStore`/`DefaultLlmResolver`/`ExchangePriceProvider`/`ExchangeMarketDataProvider`） |

### 交易所扩展性设计

virs-ccxt 采用适配器模式，每个交易所实现独立隔离。当前仅 Binance 已实现，Bybit/OKX 为计划中。添加新交易所的步骤：

1. 在 `adapter/` 下创建新模块（如 `adapter/bybit/mod.rs`）
2. 实现 `Exchange` trait
3. 在 `adapter/mod.rs` 中注册模块
4. 在 `create_exchange()` 工厂函数中添加 match 分支

所有交易所特定逻辑（REST 签名、WS 解析、错误映射）完全隔离在各自模块中，共享统一的 `Exchange` trait 接口。

### 当前完成状态

- Monorepo 架构：**已完成**
- DAG 无环：**已验证**
- 依赖清理（tokio 从 virs-types 直接依赖中移除）：**已完成**；全仓 trait 和公共函数统一返回 `VirsResult`，anyhow 仅在 `VirsError::Other(#[from] anyhow::Error)` 转换路径中保留
- 配置统一消费：**已完成**；`AppConfig.paper` 在引擎层统一使用，不再直接读取环境变量
- AiConfig 冗余定义：**已修复**；唯一定义在 virs-config，virs-types 中的冗余副本已删除
- virs-position/virs-bot 冗余 thiserror 依赖：**已清理**
- 统一错误处理：**已完成**；virs-error 作为 Layer 0 集中定义所有错误类型（`BotError`/`PositionEngineError`/`ExchangeError`/`VirsError`），各 crate 直接从 virs-error 导入，不做 re-export；virs-types/virs-ccxt 中的原定义已删除；全仓所有 trait（`GridStore`/`AutoStore`/`KlineSource`/`OrderExecutor` 等）和公共函数统一返回 `VirsResult<T>` 或具体 `Result<T, XxxError>`；`VirsError` 含 `Database(#[from] sqlx::Error)` 变体，DB 错误通过 `?` 自动归类为 503 可重试；virs-utils/virs-config/virs-market/virs-bot 全部接入 virs-error，DAG 全边落地
- Docker 一键启动：**已完成**
- 前端：**已有**（React + Vite）
- 所有 TODO 接口实现：**已完成**（Bybit/OKX 交易所适配器为计划中，架构已就绪）

### 注释掉的 `virs-ai`

workspace 中有 `# "crates/libs/virs-ai"` 被注释，标注 "Phase 5+"。当前 AI 能力（LLM 调用、提示工程）内嵌在 `virs-bot/common/ai_client.rs` 中。如果未来需要多策略共享 AI 基础设施（如 RAG、多模型路由），可提取为独立 crate。
